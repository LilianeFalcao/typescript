import styled from "styled-components"
import { colors } from "styles/globalStyle"

export const AnimeM = styled.main`
    div{
        display: flex;
        flex-direction: row;
        color: ${colors.white};
    }
    h2{
        color: ${colors.white};
    }
`